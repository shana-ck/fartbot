import { AtpAgent, type AtpAgentOptions } from "@atproto/api";
import type { RateLimitThreshold } from "rate-limit-threshold";
export declare class RateLimitedAgent extends AtpAgent {
    private limiter;
    constructor(options: AtpAgentOptions, limiter: RateLimitThreshold);
}
